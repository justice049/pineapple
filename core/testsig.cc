#include<iostream>
#include<unistd.h>
#include<cstdio>
#include<sys/types.h>
#include<sys/wait.h>

int main()
{
    // //signal(2,handler);      //自定义捕捉
    // signal(2,SIG_IGN);      //忽略的一个信号
    // signal(2,SIG_DFL);      //信号的默认处理动作
    
    return 0;
}